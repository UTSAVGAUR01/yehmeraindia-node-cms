# Astro Frontend

Run with backend server:

1. Start backend from repo root: `npm install && npm start`
2. Start frontend: `cd frontend && npm install && npm run dev`

The frontend proxies `/api/*` to `http://localhost:3000`.
