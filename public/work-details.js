(() => {
  "use strict";
  // Disabled: this legacy enhancer removed and moved React-owned card nodes.
  // Books, plays, purchase links and event details remain available through
  // the native React interface and backend APIs.
})();
